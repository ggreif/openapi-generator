TODO: Fix Motoko generator bugs found during Yamaha API testing (2026-02-05)

## 1. oneOf Handling Bug

Issue: Generator creates empty record {} for oneOf parameter types instead of proper variant types.

Example: SetVolumeVolumeParameter in the spec uses oneOf with:
- type: integer, minimum: 0
- type: string, enum: [up, down]

Expected: Variant type { #integer : Nat; #up; #down }
Generated: Empty record {}

Manual Fix Applied: Updated SetVolumeVolumeParameter.mo to include proper variant types in both the main type and JSON submodule, with conversion functions using Int.abs for integer validation.

IMPORTANT: The generator should deduce Motoko types from OpenAPI constraints:
- 'type: integer, minimum: 0' should map to Nat (not Int)
- 'type: integer' without minimum: 0 should map to Int
- This is the standard OpenAPI way to express non-negative integers
- The generator can inspect the 'minimum' constraint to choose the best-fitting Motoko type
- Conversion functions (fromJSON) should validate the constraint and reject negative values

## 2. Inline Enum Bug

Issue: When parameters have inline enum definitions (not separate models), the generator:
1. Creates switch statements with variant patterns (correct)
2. BUT types the parameter as Text instead of a variant type (incorrect)

Result: Type error: 'variant pattern cannot consume expected type Text'

Example: ZoneApi.mo:227 - The input parameter in setInput function:
- Should be: variant type { #net_radio; #napster; #spotify; #juke; #qobuz; #tidal; #deezer; #server; #bluetooth; #airplay; #mc_link; #usb }
- Generated as: Text
- But code contains: switch (input) { case (#net_radio) net_radio; case (#napster) napster; ... }

Similar Issue: PowerApi.mo:88 had broken switch statement trying to match variant patterns against Text parameter. Fixed manually by removing the switch and using the Text value directly.

## Root Cause

These are generator-level bugs in MotokoClientCodegen.java:
- oneOf types are not properly converted to Motoko variants
- Inline enum parameters don't generate corresponding model types or use variant types in function signatures
- Mismatch between generated type signatures (Text) and generated code logic (variant pattern matching)
- Generator doesn't inspect OpenAPI constraints (minimum, maximum) to deduce optimal Motoko types

## Status

- SetVolumeVolumeParameter: Manually fixed
- PowerApi inline enums: Manually fixed
- ZoneApi inline enums: Not yet fixed (typecheck currently fails on line 227)
- Generator fixes: Required in Java codebase
